# 일자리 비교 앱 — 개발 체크리스트

`통합개발계획서.md`의 실행 목록. 위에서부터 순서대로 진행한다.
각 Phase는 **완료 판정**을 통과해야 다음으로 넘어간다.

---

## P0. 계산 엔진 (UI 없음) — 최우선

- [ ] `score.js` 파일 1개 생성 (순수 함수만, DOM 접근 금지)
- [ ] `monthlyWorkHours(start, end, breakMin, daysPerWeek)` — 야간 교대(퇴근 < 출근) 처리 포함
- [ ] `monthlyCommuteHours(oneWayMin, daysPerWeek)`
- [ ] `realWage(pay, workHours, commuteHours)` — 실질시급
- [ ] `payScore()` — 50~100 밴드 상대 정규화, 공고 1개면 100 반환
- [ ] `commuteScore(x, T)` — 초과 1분당 -2
- [ ] `endTimeScore(x, H)` — 초과 1분당 -1
- [ ] `startTimeScore(x, E)` — 부족 1분당 -1
- [ ] `daysScore(x, D)` — 초과 1일당 -30
- [ ] `intensityScore(level)` — 100/80/60/35/10
- [ ] `stabilityScore(type, insurance)` — 고용형태 + 4대보험 가산
- [ ] `totalScore(jobs, weights)` — 별점 0 항목은 분모에서 제외
- [ ] `applyHardFilters(jobs, limits)` — 탈락 사유 문자열까지 반환
- [ ] `explain(ranked, weights)` — 1위 근거 문장 템플릿 조립
- [ ] **자체 테스트** `score.test.js` (assert 기반, 프레임워크 없음)
  - [ ] 손계산 예시 3개 공고의 총점 일치
  - [ ] 공고 1개일 때 0으로 나누기 없음
  - [ ] 별점 전부 0일 때 예외 처리
  - [ ] 야간 교대(22:00~06:00) 근무시간 8시간으로 계산
  - [ ] 전 공고 탈락 시 빈 배열 + 사유 반환

**완료 판정:** `node score.test.js` 전부 통과 + 손계산과 점수 일치

---

## P1. 공고 등록 + 저장 + 순위

- [ ] `index.html` 1개 파일 생성 (외부 라이브러리 0개)
- [ ] 공고 등록 폼 — 기본 6항목 (회사명 / 급여 / 출근 / 퇴근 / 주 근무일 / 편도 통근)
- [ ] 선택 항목 접힘 영역 (휴게시간 / 업무강도 / 고용형태 / 4대보험)
- [ ] 시급 ↔ 월급 전환 토글
- [ ] `localStorage` 저장·불러오기
- [ ] 공고 수정 / 삭제
- [ ] 순위 목록 렌더링 (적합도 점수 + 실질시급 병기)
- [ ] 입력값 검증 (음수·빈값·시각 형식)

**완료 판정:** 새로고침해도 공고가 남아 있고 순위가 그대로 나온다

---

## P2. 중요도 별점 + 즉시 재정렬

- [ ] 7개 항목 별점 UI (0~5, 터치로 조절)
- [ ] 별점 변경 시 순위 실시간 재계산·재정렬
- [ ] 별점 `localStorage` 저장
- [ ] 별 0개 = "이 항목 안 봄" 시각적으로 명확히 표시

**완료 판정:** 별 하나 바꾸면 0.1초 내에 순위가 눈에 보이게 바뀐다

---

## P3. 필수조건 (탈락 필터)

- [ ] 필수조건 입력 4개 (최저 월급 / 최대 퇴근 / 최대 통근 / 최대 근무일)
- [ ] 미설정 = 제한 없음으로 동작
- [ ] 탈락 공고를 목록 하단에 회색 + 사유 표시 (숨기지 않는다)
- [ ] 전부 탈락 시 "조건이 너무 엄격합니다" + 어떤 조건을 풀면 몇 개가 살아나는지 안내

**완료 판정:** 탈락 공고가 사라지지 않고 이유와 함께 보인다

---

## P4. 실질시급 + 근거 문장

- [ ] 각 공고 카드에 실질시급 상시 표시
- [ ] 명목시급 vs 실질시급 차이 안내 문구
- [ ] "월급은 A가 높지만, 출퇴근까지 치면 B가 시간당 OOO원 더 받습니다" 자동 생성
- [ ] 1위 근거 한 줄 자동 생성
- [ ] 월 통근 손실시간 표시 ("이 직장은 출퇴근에 월 44시간을 씁니다")

**완료 판정:** 사용자가 순위 이유를 물어볼 필요가 없다

---

## P5. 나란히 비교표

- [ ] 공고 3~5개 가로 비교 테이블
- [ ] 항목별 실제 값 + 점수 동시 표시
- [ ] CSS 가로 막대로 점수 시각화 (라이브러리 없음)
- [ ] 모바일 가로 스크롤 처리
- [ ] 항목별 최고값 강조 표시

**완료 판정:** 휴대폰 세로 화면에서 공고 3개가 읽힌다

> **여기까지가 1차 출시선.**

---

## P6. 템플릿 4종

- [ ] 육아 병행형 / 돈 우선형 / 몸 편한 일형 / 워라밸형 버튼
- [ ] 버튼 1번에 별점 7개 자동 세팅
- [ ] 템플릿 적용 후 개별 조정 가능
- [ ] 첫 방문 시 템플릿 선택 화면 노출

**완료 판정:** 앱 열고 3초 안에 기준 설정이 끝난다

---

## P7. PWA + 백업 + 공유

- [ ] `manifest.json` (이름 / 아이콘 / 테마색 / standalone)
- [ ] `sw.js` 서비스워커 — 앱 파일 캐시
- [ ] 홈화면 추가 안내 (안드로이드 / iOS 각각)
- [ ] JSON 내보내기 버튼
- [ ] JSON 가져오기 버튼 (형식 검증 포함)
- [ ] 결과를 URL 해시에 담아 링크 생성
- [ ] 공유 링크로 열면 읽기 전용 결과 화면
- [ ] `navigator.share` 지원 시 네이티브 공유 시트 사용

**완료 판정:** 비행기모드에서 앱이 정상 동작한다

---

## P8. 배포 + 검증

- [ ] GitHub 저장소 생성 (public)
- [ ] GitHub Pages 활성화
- [ ] 실제 휴대폰(안드로이드 1대 + 아이폰 1대)에서 동작 확인
- [ ] 앱 하단에 "이 점수는 회사 평가가 아니라 내 기준 적합도입니다" 고정 문구
- [ ] 계산식 공개 페이지 (신뢰 확보용)
- [ ] 지인 5명 테스트
  - [ ] 실제 채용공고 3개를 보여주고 **먼저 직접 순위**를 매기게 함
  - [ ] 그다음 앱으로 같은 공고를 비교
  - [ ] "앱 순위가 내 생각과 비슷했나?" 확인
  - [ ] "입력하다 그만두고 싶었던 지점"을 반드시 질문
- [ ] 성공 지표 3개 측정 (입력 완주율 / 결과 도달율 / 재방문율)

**완료 판정:** 지인 5명 중 3명 이상이 "순위가 내 생각과 비슷하다"고 답한다

---

## P9. Play 스토어 등록 (등록비 기결제 — 미룰 이유 없음)

- [ ] `bubblewrap init --manifest={PWA URL}/manifest.json` 실행
- [ ] 생성된 APK/AAB 로컬 설치로 동작 확인
- [ ] 기존에 $25 결제된 Google Play Console 계정으로 업로드
- [ ] 스토어 등록정보(설명·스크린샷·개인정보처리방침 링크) 작성
- [ ] 심사 제출 (URL만 감싼 TWA라 반려 사유가 거의 없음)
- [ ] 심사 통과 후 실기기에서 스토어 설치 버전 동작 확인

**완료 판정:** Play 스토어 검색으로 앱이 설치되고, 내용은 웹배포(P8)와 100% 동일하게 갱신된다 (앱 재빌드 불필요)

---

## 2단계 (MVP 검증 통과 후에만)

- [ ] 공고 텍스트 붙여넣기 → 정규식 규칙 기반 자동 추출 (AI 아님, 비용 0)
- [ ] 파싱 결과 수동 보정 UI
- [ ] 자주 쓰는 사이트 3곳 포맷 우선 대응
- [ ] 비교 이력 저장 / 즐겨찾기 / 공고 복제

## 3단계 (사용자 확보 후에만)

- [ ] AdSense (순위에 영향 없는 위치)
- [ ] OCR / AI 파싱 (수익 발생 이후)

---

## 절대 하지 않을 것 (범위 방어선)

- ❌ 채용사이트 크롤링 / 자동 수집
- ❌ 회원가입 · 로그인 · 서버 · DB
- ❌ MVP 단계 AI API 호출
- ❌ 차트 라이브러리 등 외부 의존성 추가
- ❌ 평가 항목 7개 초과 확장
- ❌ "좋은 회사 점수"라는 표현 사용
