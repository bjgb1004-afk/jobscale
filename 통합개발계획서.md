# 일자리 비교·선택 앱 — 통합 개발계획서

작성일: 2026-09-10
기준 문서: `gemini.txt`, `gpt.txt`, `일자리비교앱_개발기획서.txt` 3종 통합
설계 원칙: **대중성 최우선 · 개발비/유지비 0원 · 품질은 타협 없음**

---

## 0. 3개 문서 검토 결과 (채택 / 기각)

세 문서는 핵심(개인 가중치 채점 → 순위)에서 완전히 일치한다.
차이는 "어디까지 만들 것인가"에 있고, 아래처럼 정리했다.

| 항목 | 출처 | 판정 | 이유 |
|---|---|---|---|
| 개인 가중치 기반 채점·순위 | 3개 공통 | **채택 (핵심)** | 이 앱의 존재 이유 |
| 필수조건(탈락 필터)과 선호조건 분리 | gpt | **채택 (핵심)** | 실제 구직은 "양보 불가 조건"이 먼저 걸러진다 |
| 실질시급(출퇴근 포함 시간당 보상) | gpt + 기획서 | **채택 (킬러 지표)** | 다른 앱에 없는 유일한 무기 |
| 가중치 바꾸면 즉시 재정렬 | gpt | **채택** | 이 앱의 재미이자 재방문 이유 |
| 유형별 템플릿 버튼 | gemini + 기획서 | **채택** | 초기 이탈 방지의 핵심 |
| 점수 옆에 실제 값 병기 | 기획서 + gpt | **채택** | 점수만 보면 아무도 신뢰 안 함 |
| "내 기준 적합도" 표현 | gpt | **채택** | "좋은 회사 점수"는 부정확하고 분쟁 소지 |
| 결과 공유 | gemini(스크린샷) | **채택하되 변경** | 이미지 저장 대신 **링크 공유**(데이터를 URL에 담음) → 서버 0원으로 공유 |
| 레이더 차트 | gemini | **기각 → 대체** | 차트 라이브러리 의존성 발생. CSS 가로 막대로 대체 (항목 7개는 막대가 더 읽기 쉬움) |
| 네이티브 앱(React Native/Expo) | gemini + gpt | **기각 → PWA + TWA** | Google Play 등록비 $25는 **이미 결제 완료(추가비용 0)**. 하지만 RN은 SDK 업그레이드·빌드 쿼터·심사 대기라는 *지속* 비용이 남는다. PWA로 만들고 **TWA로 감싸서** 같은 Play 계정에 올리면 코드 1벌로 웹+스토어 둘 다 확보 (7장 참조) |
| Supabase / 로그인 / 서버 | gemini | **기각(MVP)** | 무료 티어도 휴면 정지·한도 관리 필요. 로컬 저장으로 충분 |
| 공고 URL 크롤링·자동 수집 | gemini | **기각** | 이용약관·재배포 문제 + 크롤러 영구 유지보수 |
| 이미지 OCR 자동 인식 | 기획서 | **기각(MVP)** | 유료 API. 3단계 이후 검토 |
| AI 자동 파싱 | gemini + 기획서 | **기각(MVP)** | API 비용 발생. 규칙 기반 붙여넣기 파싱으로 2단계에 대체 |
| 가족 귀가시간 겹침 계산 | 기획서 | **기각 → 단순화** | 입력 부담이 계산 가치보다 큼. "희망 퇴근시각" 하나로 90% 커버 |
| 광고(AdMob) | gemini | **보류** | 웹은 AdSense이고 승인에 트래픽 필요. 사용자 확보 후 |
| 익명 커뮤니티/통계 | gemini | **기각** | 서버 필수 + 운영 부담. 목표와 정반대 |

---

## 1. 제품 한 줄 정의

> **내가 중요하게 보는 조건의 비중을 정하면, 등록한 채용공고들을 내 기준으로 채점해 순위를 매겨주는 계산기.**

- 채용공고를 **찾는** 앱이 아니다 (알바몬·사람인의 영역).
- 찾은 다음 **고르는** 걸 돕는 앱이다.
- 포지션 한 줄: **"나에게 맞는 직장 계산기"**

---

## 2. 설계 원칙 4가지

1. **첫 결과까지 60초.** 템플릿 버튼 1개 + 공고 2개 입력이면 끝. 회원가입·설정 없음.
2. **모든 점수는 근거와 함께.** 점수 옆에 항상 실제 값(월급, 통근 35분 등)을 붙인다.
3. **의존성 0.** 라이브러리·서버·API·계정 전부 없음. 파일 하나가 앱 전체.
4. **입력 항목은 늘리지 않는다.** 항목이 늘면 정확도가 아니라 이탈률이 오른다.

---

## 3. MVP 범위 확정

### 포함
- 유형별 템플릿 4종 (버튼 1번으로 시작)
- 평가항목 7개 중요도 설정 (별 1~5)
- 필수조건 4개 (위반 시 탈락)
- 공고 등록 (기본 6항목 + 선택 4항목)
- 자동 채점 · 순위
- 실질시급 자동 계산
- 항목별 점수 + 실제 값 나란히 표시
- 1위 근거 한 줄 자동 생성
- 공고 나란히 비교표
- 로컬 자동 저장 (브라우저)
- 오프라인 동작 (PWA)
- 결과 링크 공유
- 백업/복원 (JSON 파일 내보내기·가져오기)

### 제외 (의도적)
크롤링 · AI · OCR · 회원가입 · 서버 · DB · 결제 · 광고 · 이력서 · 채팅 · 기업 기능 · 추천 알고리즘

---

## 4. 평가항목 최종 확정 (7개)

기획서 "다음에 할 일 → 기준 항목 최종 확정"에 대한 답이다.
후보 12개 이상 중 **입력 부담 대비 변별력** 기준으로 7개만 남겼다.

| # | 항목 | 사용자 입력 | 점수 성격 |
|---|---|---|---|
| 1 | 급여 (→ 실질시급) | 월급 또는 시급 | 높을수록 좋음 (상대비교) |
| 2 | 통근시간 | 편도 분 | 낮을수록 좋음 (절대기준) |
| 3 | 퇴근시각 | 시:분 | 사용자 선호 시각 기준 |
| 4 | 출근시각 | 시:분 | 사용자 선호 시각 기준 |
| 5 | 주 근무일수 | 일 | 사용자 희망 기준 |
| 6 | 업무강도 | 5단계 선택 | 등급 매핑 |
| 7 | 고용안정·복지 | 고용형태 + 4대보험 | 등급 매핑 |

**탈락시킨 후보와 이유**
- 창업 연계 가능성 → 신뢰할 데이터가 없음 (기획서도 "추정치" 한계 인정)
- 정신적 업무강도 → 육체강도와 사용자 응답이 사실상 중복
- 야간근무 여부 → 퇴근시각 점수로 자동 커버
- 초보 가능 여부 → 점수가 아니라 **탈락 조건**의 성격 → 필수조건으로 이동
- 가족 겹침시간 → 입력 부담이 계산 가치보다 큼

---

## 5. 계산 엔진 명세 ★핵심★

UI보다 이걸 먼저 만들고 먼저 검증한다.

### 5-1. 전처리: 실근무시간

```
1일 실근무시간 = (퇴근시각 - 출근시각) - 휴게시간      // 휴게 미입력 시 0
월 근무시간   = 1일 실근무시간 × 주 근무일수 × 4.345
월 통근시간   = (편도분 × 2 ÷ 60) × 주 근무일수 × 4.345
```

### 5-2. 급여 정규화와 실질시급

```
월급 입력 → 그대로 사용
시급 입력 → 월급 = 시급 × 월 근무시간

명목 환산시급 = 월급 ÷ 월 근무시간
실질 시급     = 월급 ÷ (월 근무시간 + 월 통근시간)     ← 킬러 지표
```

**표시 예시** (반드시 이 형태로)
> A사 월급 300만 · 실질시급 13,636원
> B사 월급 270만 · 실질시급 14,439원
> → **"월급은 A가 30만 원 높지만, 출퇴근까지 내 시간으로 치면 B가 시간당 803원 더 받습니다."**

### 5-3. 항목별 점수 (0~100)

**① 급여 — 등록 공고 간 상대 정규화**
```
score = (max == min) ? 100 : 50 + 50 × (x - min) / (max - min)
```
- 기준값은 **실질시급**을 쓴다 (월급 총액이 아니라).
- 0~100이 아니라 **50~100 밴드**를 쓴다. 최하위 공고에 0점을 주면 "탈락"처럼 보여 결과를 안 믿는다.
- 공고가 1개면 max == min → 100점 (0으로 나누기 방지).

**② 통근시간 — 사용자 희망 편도시간 T (기본 30분)**
```
x <= T  →  100
x >  T  →  max(0, 100 - (x - T) × 2)      // 초과 1분당 2점
예) T=30 → 45분:70점, 60분:40점, 80분 이상:0점
```

**③ 퇴근시각 — 사용자 희망 퇴근시각 H (기본 18:00)**
```
x <= H  →  100
x >  H  →  max(0, 100 - 초과분)            // 초과 1분당 1점
예) H=18:00 → 18:30:70점, 19:00:40점, 19:40 이후:0점
```

**④ 출근시각 — 사용자가 가능한 가장 이른 출근시각 E (기본 09:00)**
```
x >= E  →  100
x <  E  →  max(0, 100 - 부족분)            // 1분 이를수록 1점
```

**⑤ 주 근무일수 — 사용자 희망 D (기본 5일)**
```
x <= D  →  100
x >  D  →  max(0, 100 - (x - D) × 30)     // 6일:70점, 7일:40점
```

**⑥ 업무강도 — 고정 매핑**
```
매우 편함 100 / 편함 80 / 보통 60 / 힘듦 35 / 매우 힘듦 10
```

**⑦ 고용안정·복지 — 가산 합계**
```
고용형태:  정규직 50 / 계약직 30 / 일용·단기 10
4대보험:   있음 +50 / 없음 +0
→ 0~100
```

### 5-4. 가중치 계산

```
사용자가 항목별로 별 1~5개 선택 (별 0개 = 이 항목 안 봄)
weight_i = star_i ÷ Σ(star)
총점     = Σ(score_i × weight_i)
```
- 별 0개 항목은 계산에서 완전히 제외 (분모에서도 뺀다).
- 결과 표기는 **"내 기준 적합도 87점"** (❌ "좋은 회사 87점" 금지)

### 5-5. 필수조건 (하드 필터)

점수와 **완전히 분리**한다. 하나라도 위반하면 순위에서 제외.

| 조건 | 기본값 |
|---|---|
| 최저 월급 | 미설정 |
| 최대 퇴근시각 | 미설정 |
| 최대 편도 통근시간 | 미설정 |
| 최대 주 근무일수 | 미설정 |
| (선택) 초보 가능 필수 | 미설정 |

> 탈락 공고는 **숨기지 않는다.** 목록 하단에 회색으로 두고 이유를 붙인다.
> `B사 — 탈락 (퇴근 19:00 > 내 한계 18:00)`
> 숨기면 "내가 넣은 게 왜 없어졌지?" 하고 앱을 의심한다.

### 5-6. 근거 문장 자동 생성

1위 공고에서 `score_i × weight_i` 기여도 상위 2개 항목을 뽑아 조립한다.

```
"{1위}가 {항목1}과 {항목2}에서 당신 기준에 가장 잘 맞습니다.
 단, {최저점 항목}은 {2위} 대비 낮습니다."
```
템플릿 문자열 조립이면 충분하다. AI 필요 없음.

### 5-7. 엣지 케이스 (반드시 처리)

- 공고 0개 → 안내 화면
- 공고 1개 → 상대 정규화 항목 전부 100점 + "비교하려면 1개 더 등록하세요" 안내
- 전 공고가 필수조건 탈락 → "조건이 너무 엄격합니다" + 완화 제안
- 별점 전부 0 → 저장 막고 최소 1개 요구
- 퇴근시각 < 출근시각 (야간 교대) → 24시간 더해 계산
- 총점 동점 → 실질시급 높은 쪽이 상위

---

## 6. 화면 구성 (4개)

gpt안 6개를 4개로 합쳤다. 화면이 줄면 이탈도 준다.

### ① 시작 / 홈
```
나에게 맞는 직장 계산기

어떤 게 제일 중요하세요?
[👶 육아 병행형] [💰 돈 우선형]
[🛋 몸 편한 일형] [⚖ 워라밸형]
[✏ 직접 설정]

─ 내 공고 (4개) ─
🥇 C사   내 기준 적합도 91점   실질시급 14,439원
🥈 B사   86점                  13,900원
🥉 A사   82점                  13,636원
   D사   탈락 — 퇴근 19:30 > 내 한계 18:00

[+ 공고 추가]  [나란히 비교]  [기준 바꾸기]
```

### ② 내 기준 (중요도 + 필수조건 한 화면)
```
중요도                        필수조건 (넘으면 탈락)
💰 급여       ★★★★★          최저 월급    [     ] 만원
🚗 통근       ★★★★☆          최대 퇴근    [18:00]
🕕 퇴근시각   ★★★★☆          최대 통근    [ 60  ] 분
🕘 출근시각   ★★★☆☆          최대 근무일  [  5  ] 일
📅 근무일수   ★★★☆☆
💪 업무강도   ★★★☆☆
🛡 고용안정   ★★☆☆☆
```
> 별을 움직이는 **즉시 아래 순위가 다시 정렬**된다. (이 앱의 핵심 경험)

**템플릿 4종 기본값**

| 항목 | 육아 병행형 | 돈 우선형 | 몸 편한 일형 | 워라밸형 |
|---|---|---|---|---|
| 급여 | ★★★ | ★★★★★ | ★★★ | ★★★ |
| 통근 | ★★★★★ | ★★ | ★★★ | ★★★★ |
| 퇴근시각 | ★★★★★ | ★ | ★★★ | ★★★★ |
| 출근시각 | ★★★★ | ★ | ★★ | ★★★ |
| 근무일수 | ★★★★ | ★ | ★★★ | ★★★★★ |
| 업무강도 | ★★★ | ★ | ★★★★★ | ★★★ |
| 고용안정 | ★★ | ★★ | ★★★ | ★★★★ |

### ③ 공고 등록
```
[기본] — 이것만 넣어도 저장됨
회사명 [          ]   월급 [    ] 만원 (또는 시급 전환)
근무   [09:00] ~ [18:00]    주 [5] 일
통근   편도 [35] 분

[선택] ▼ 펼치기
휴게시간 [60] 분     업무강도 [보통 ▾]
고용형태 [정규직 ▾]  4대보험 [☑]
```

### ④ 나란히 비교
```
                A사      B사      C사
월급            300만    270만    290만
실질시급        13,636   14,439   14,010
근무            09~18    08~16    10~19
통근            35분     15분     50분
근무일          5일      5일      6일
업무강도        보통     편함     힘듦
────────────────────────────────────────
내 기준 적합도   82       86       91
```
각 항목 점수는 CSS 가로 막대로 시각화. (차트 라이브러리 없음)

---

## 7. 기술 구성 및 비용

### 구조
```
단일 HTML 파일 (index.html)
 ├─ HTML + CSS + Vanilla JS   (외부 라이브러리 0개)
 ├─ localStorage              (공고·기준 저장, 서버 없음)
 ├─ manifest.json + sw.js     (PWA: 홈화면 추가 + 오프라인)
 └─ GitHub Pages              (호스팅)
```

### 왜 네이티브(React Native) 대신 "PWA + TWA"인가

**Google Play 등록비 $25는 이미 결제됨 — 그 비용은 빼고 비교한다.** 남는 차이는 *지속* 비용이다.

| | 네이티브 앱 (RN/Expo) | PWA 단독 | **PWA + TWA (채택)** |
|---|---|---|---|
| 최초 등록비 | 기결제 | 0원 | 기결제 (동일 계정 재사용) |
| 심사 | 수정마다 대기 | 없음 | **최초 1회만** (TWA는 URL만 감싸므로 통과 쉬움) |
| 빌드 쿼터 | EAS 무료 빌드 제한 있음 | 없음 | **없음** (재빌드가 사실상 불필요 — 앱 내용은 웹에서 즉시 갱신됨) |
| 설치 장벽 | 스토어 검색 → 다운로드 | 링크 클릭 → 즉시 사용 | **둘 다 가능** (Play 검색 노출 + 링크 공유) |
| 코드 유지 | UI 코드 이중 관리 필요 | 코드 1벌 | **코드 1벌** (TWA는 껍데기일 뿐, 로직은 웹 그대로) |
| 결과 공유 | 앱 설치 유도 필요 | 링크 하나로 공유 | 링크 하나로 공유 |

> 이 앱은 카메라·푸시·백그라운드 실행이 필요 없다 → React Native로 개발할 이유가 없다.
> 대신 **PWA로 전부 만들고, 완성 후 [Bubblewrap](https://github.com/GoogleChromeLabs/bubblewrap)(구글 공식 CLI, 무료)으로 TWA 패키징해서 기결제된 Play 계정에 올린다.** 코드를 다시 짜지 않고 웹앱 그대로 스토어 노출도 확보하는 구조 — "PWA냐 스토어냐" 양자택일이 아니라 **PWA 개발 = 스토어 출시 준비**가 된다. (P7~P8에 반영, 8장 참조)

### 비용 계산 — 무료 한도 소진 시점까지 산출

| 항목 | 한도 | 예상 사용 | 소진 시점 |
|---|---|---|---|
| GitHub Pages 저장소 | 1 GB | 약 200 KB | **소진 불가** |
| GitHub Pages 대역폭 | 소프트 100 GB/월 | 신규 방문 1회 ≈ 100 KB, 재방문은 PWA 캐시로 0 KB | 100 GB ÷ 100 KB = **월 약 100만 신규 방문에서 소진** → 개인 앱 규모에선 도달 불가 |
| GitHub Pages 빌드 | 시간당 10회 | 수정할 때만 push | **소진 불가** (하루 10번 고쳐도 한도의 1/24) |
| 도메인 | `아이디.github.io` 무료 | 커스텀 도메인 미사용 | **0원** (커스텀 쓰면 연 1~2만 원 발생 → 안 씀) |
| 서버 / DB / AI API | 사용 안 함 | 0 | **0원** |

**총 유지비: 월 0원 · 총 개발비: 0원(직접 개발).**
사용자가 10만 명이 되어도 0원이 유지되는 구조다.

### 데이터 저장·이동
- 저장: `localStorage` (공고 + 기준 JSON)
- 백업: **JSON 파일 내보내기 / 가져오기 버튼** → 서버 없이 기기 이동 가능
- 공유: 데이터를 URL 해시(`#`)에 담아 링크 생성 → 받는 사람은 설치 없이 결과 확인
- 개인정보를 서버로 전혀 보내지 않으므로 개인정보처리방침 부담도 최소

---

## 8. 개발 순서

계산 엔진 → UI 순서. 반대로 하면 반드시 다시 만들게 된다.

| Phase | 내용 | 완료 판정 |
|---|---|---|
| **P0** | 계산 엔진 (순수 JS 함수 + 자체 테스트) | 예시 공고 3개의 점수가 손계산과 일치 |
| **P1** | 공고 등록 + localStorage + 순위 표시 | 새로고침해도 데이터 유지 |
| **P2** | 중요도 별점 UI + 즉시 재정렬 | 별 바꾸면 0.1초 내 순위 변경 |
| **P3** | 필수조건 + 탈락 표시 | 탈락 공고가 사유와 함께 하단 표시 |
| **P4** | 실질시급 + 근거 문장 | "월급은 A가 높지만…" 문장 자동 생성 |
| **P5** | 나란히 비교표 + CSS 막대 | 공고 3개 한 화면 비교 |
| **P6** | 템플릿 4종 | 버튼 1번으로 별점 자동 세팅 |
| **P7** | PWA(오프라인·홈화면) + 백업/복원 + 링크 공유 | 비행기모드에서 동작 |
| **P8** | 배포(GitHub Pages) + 지인 5명 테스트 | 실사용 피드백 수집 |
| **P9** | Bubblewrap TWA 패키징 → **기결제된 Play 계정**에 등록 | Play스토어에서 검색·설치된다 |

**P0~P5가 1차 출시선.** P6~P9은 그 뒤 약 1~2주.
> P9은 등록비가 이미 지불돼 있으므로 미룰 이유가 없다. PWA(P7)가 끝나는 즉시 붙인다 — Bubblewrap이 `manifest.json`을 읽어 자동으로 APK를 만들어 주므로 추가 코드가 없다.

### 2단계 (MVP 검증 후에만)
- 공고 텍스트 붙여넣기 → **규칙 기반** 자동 파싱 (정규식. AI 아님 → 비용 0)
- 파싱 실패 시 수동 보정 UI 필수
- 비교 이력 저장, 즐겨찾기, 공고 복제

### 3단계 (사용자 확보 후에만)
- AdSense (순위에 절대 영향 없는 위치)
- OCR / AI 파싱 (유료 → 수익 발생 이후)

---

## 9. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| **입력이 귀찮아서 이탈** (최대 위험) | 필수 입력 6개로 제한, 나머지는 접힌 선택 영역. 공고 1개만 넣어도 저장 가능 |
| 기준 설정이 귀찮음 | 템플릿 버튼 4개로 3초 시작. 세부 조정은 원하는 사람만 |
| 점수를 신뢰하지 않음 | 점수 옆 실제 값 병기 + 1위 근거 문장 + 계산식 공개 페이지 |
| 상대평가라 헷갈림 | 급여만 상대비교(50~100 밴드), 나머지는 절대기준. 화면에 "등록한 공고끼리 비교한 값" 명시 |
| "객관적 회사 평가"로 오해 | 모든 표기를 **"내 기준 적합도"**로 통일 + 하단 고정 문구 명시 |
| 브라우저 데이터 삭제로 유실 | JSON 백업 버튼 상시 노출 + 첫 저장 시 1회 안내 |
| 바이럴 실패 | 결과 링크 공유. "나 여기 갈까 저기 갈까?"를 배우자·지인에게 물어보는 상황이 실제 사용 맥락 |

---

## 10. 성공 판단 기준

기능을 더 붙일지, 문제 정의로 돌아갈지를 이 3개로만 결정한다.

1. **입력 완주율** — 방문자 중 공고를 **2개 이상** 등록한 비율 ≥ 40%
2. **결과 도달율** — 공고 등록자 중 순위를 보고 **비교표까지 연** 비율 ≥ 50%
3. **재방문율** — 7일 내 다시 열어본 비율 ≥ 20%

> 1번 미달 → 입력 UI 문제. 항목을 더 줄인다.
> 2번 미달 → 결과 신뢰도 문제. 근거 표시를 강화한다.
> 3번 미달 → 1회성 도구라는 뜻. 기능 추가가 아니라 문제 정의를 다시 본다.

측정은 서버 없이 시작한다. 로컬 카운터 + 지인 테스트 관찰(5명). 트래픽이 붙으면 무료 분석 도구를 검토한다.

---

## 11. "다음에 할 일" 처리 결과 (기획서 10장에 대한 답)

- [x] 기준 항목 최종 확정 → **7개** (4장)
- [x] 유형별 템플릿 설계 → **4종** (6장 ②)
- [x] 화면 흐름 그리기 → **4화면** (6장)
- [ ] P0 계산 엔진 개발 착수 ← **여기서 시작**
- [ ] 지인 5명 테스트

상세 실행 목록은 `개발체크리스트.md` 참조.
